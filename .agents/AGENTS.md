# Core Project Rules

- **No Hardcoded Text**: Never hardcode user-facing text. Always use multilang (i18n) files to manage strings.
- **Dark and Light Mode**: Always implement both dark and light modes for all interfaces.
- **Responsive Layouts**: Always implement different screen sizes with appropriate formatting and structural changes for each device tier (mobile, tablet, desktop).
